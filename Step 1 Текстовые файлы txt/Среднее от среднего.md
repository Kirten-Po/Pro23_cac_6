f = open('data.txt')
s = map(str.strip, f.readlines())
d = []
for line in s:
    d.extend(line.split())
d = list(map(int, d))
d_avarage = round(sum(d) / len(d), 4)
d_filter = list(filter(lambda x: x > d_avarage, d))  # числа больше среднего
d_d = round(sum(d_filter) / len(d_filter), 4)
print(d_d)