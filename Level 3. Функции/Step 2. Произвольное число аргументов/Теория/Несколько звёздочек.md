В функции не может быть несколько аргументов со звёздочками, но при вызове функции эти аргументы можно передавать, распаковывая сколько угодно коллекций:

```python
arr = [1, 5, 9, 17, 29]
tpl = (3, 6, 12)
print(func(*tpl, *arr))
```